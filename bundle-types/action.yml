name: "Grafana Bundle Types"
description: "Bundles a Grafana plugins types and pushes them to npm."

outputs:
  typesFilePath:
    description: "The path to the built types file."
    value: ${{ steps.set-outputs.outputs.typesFilePath }}

inputs:
  entryPoint:
    description: "Location of types file to bundle."
    required: false
    default: "./src/types/index.ts"
  tsConfig:
    description: "A path to the tsconfig file to use when bundling types."
    required: false
    default: ""
  pluginId:
    description: "The plugins Id. Used as the directory to output the types file to."
    required: true

runs:
  using: "composite"
  steps:
    - uses: actions/setup-node@v4
      with:
        node-version: "${{ inputs.node-version }}"

    - name: Install dependencies
      run: ${{ github.action_path }}/pm.sh install
      shell: bash

    - name: Get plugin id
      run: |
        which jq
      shell: bash

    # - id: get-secrets
    #   uses: grafana/shared-workflows/actions/get-vault-secrets@main
    #   with:
    #     common_secrets: |
    #       GITHUB_APP_ID=plugins-platform-bot-app:app-id
    #       GITHUB_APP_PRIVATE_KEY=plugins-platform-bot-app:private-key

    # - name: Generate token
    #   id: generate_token
    #   uses: tibdex/github-app-token@v2
    #   with:
    #     app_id: ${{ env.GITHUB_APP_ID }}
    #     private_key: ${{ env.GITHUB_APP_PRIVATE_KEY }}

    # - name: Clone NPM types package repo
    #   run: |
    #     cd ..
    #     gh auth status
    #     gh repo clone grafana/plugin-extension-types
    #   shell: bash
    #   env:
    #     GITHUB_TOKEN: ${{ steps.generate_token.outputs.token }}

    # - name: Bundle types
    #   run: npx -y @grafana/plugin-types-bundler@latest --entry-point ${{ inputs.entryPoint }} --out-dir="../plugin-extension-types/${{ inputs.pluginId }}"
    #   shell: bash

    # - name: Create branch
    #   run: |
    #     cd ../plugin-extension-types
    #     git config --local user.name "github-actions[bot]"
    #     git config --local user.email "github-actions[bot]@users.noreply.github.com"
    #     git checkout -b test-branch
    #     git add .
    #     git commit -m "wip: testing"
    #     git push https://x-access-token:${{ steps.generate_token.outputs.token }}@github.com/grafana/plugin-extension-types test-branch
    #   shell: bash

    # - name: Create or Update PR
    #   if: steps.compare-versions.outputs.update_needed == 'true'
    #   env:
    #     GH_TOKEN: ${{ steps.generate_token.outputs.token }}
    #   run: |
    #     PR_NUMBER=$(gh pr list --base main --head update-grafana-create-plugin-${{ env.LATEST_VERSION }} --json number -q '.[0].number')

    #     PR_BODY=$(cat <<EOF
    #     Bumps [\`@grafana/create-plugin\`](https://github.com/grafana/plugin-tools/tree/main/packages/create-plugin) configuration from ${{ env.CONFIG_VERSION }} to ${{ env.LATEST_VERSION }}.

    #     **Notes for reviewer:**
    #     This is an auto-generated PR which ran \`@grafana/create-plugin update\`.
    #     Please consult the create-plugin [CHANGELOG.md](https://github.com/grafana/plugin-tools/blob/main/packages/create-plugin/CHANGELOG.md) to understand what may have changed.
    #     Please review the changes thoroughly before merging.
    #     EOF
    #     )

    #     if [ -z "$PR_NUMBER" ]; then
    #       gh pr create --base main --head update-grafana-create-plugin-${{ env.LATEST_VERSION }} --title "Chore: Bump @grafana/create-plugin configuration to ${{ env.LATEST_VERSION }}" --body "$PR_BODY"
    #     else
    #       gh pr edit $PR_NUMBER --title "Chore: Bump @grafana/create-plugin configuration to ${{ env.LATEST_VERSION }}" --body "$PR_BODY"
    #     fi
    #   shell: bash
